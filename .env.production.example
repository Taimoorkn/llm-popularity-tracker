# Vercel Production Environment Variables
# Copy this to Vercel Dashboard > Settings > Environment Variables

# Database Configuration (Required for Vercel)
# Use a managed PostgreSQL service like Vercel Postgres, Supabase, or Neon
DATABASE_URL=postgresql://username:password@host:5432/database?sslmode=require
POSTGRES_HOST=your-database-host.com
POSTGRES_PORT=5432
POSTGRES_DB=llm_tracker
POSTGRES_USER=your_username
POSTGRES_PASSWORD=your_secure_password

# Redis Configuration (Required)
# Use a managed Redis service like Upstash, Redis Cloud, or Vercel KV
REDIS_URL=redis://default:password@your-redis-host.com:6379
REDIS_HOST=your-redis-host.com
REDIS_PORT=6379
REDIS_PASSWORD=your_redis_password

# Security (Required - Generate secure random strings)
JWT_SECRET=generate-a-secure-random-string-at-least-32-chars
SESSION_SECRET=generate-another-secure-random-string-at-least-32-chars
NEXTAUTH_SECRET=generate-third-secure-random-string-at-least-32-chars

# Application Settings
NODE_ENV=production
NEXTAUTH_URL=https://your-domain.vercel.app

# Optional: Real-time Features
# NEXT_PUBLIC_PUSHER_APP_KEY=your_pusher_key
# PUSHER_APP_ID=your_pusher_app_id
# PUSHER_APP_SECRET=your_pusher_secret
# PUSHER_APP_CLUSTER=us2

# Optional: Analytics
# NEXT_PUBLIC_ANALYTICS_ID=your_analytics_id

# Rate Limiting (Optional - defaults are fine)
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW_MS=900000

# Feature Flags
ENABLE_ANALYTICS=true
ENABLE_REAL_TIME=true

# Logging Level
LOG_LEVEL=info